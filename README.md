# JJ-Media Social Audit Engine

This branch contains a verified deployment bootstrap. During dependency installation it reconstructs the complete Next.js source archive, validates its SHA-256 checksum and then runs the normal production build.
